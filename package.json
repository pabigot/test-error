{
  "name": "test-error",
  "version": "1.0.1",
  "description": "Function to check the type and message content of a Node.js Error",
  "keywords": [
    "assert",
    "unit"
  ],
  "homepage": "https://github.com/pabigot/test-error",
  "bugs": "https://github.com/pabigot/test-error/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/pabigot/test-error"
  },
  "license": "MIT",
  "author": "Peter A. Bigot",
  "main": "index.js",
  "devDependencies": {
    "coveralls": "^3.0.0",
    "istanbul": "^0.4.5",
    "mocha": "^5.0.4"
  },
  "scripts": {
    "coverage": "istanbul cover _mocha -- -u tdd",
    "coveralls": "istanbul cover _mocha --report lcovonly -- -u tdd && cat ./coverage/lcov.info | coveralls",
    "test": "mocha -u tdd"
  }
}
